# عقل لواو — Luau Mind

مساعد عربي لبرمجة روبلوكس بلغة **Luau**: يجاوب على أسئلة السكربتات، يولّد أنظمة
جاهزة، ويشرح الدروس — كله مبني يدوياً بدون أي API خارجي مدفوع، وبدعم تدريب آلي
عبر Qwen لتوسيع المعرفة على مدار الساعة.

## كيف يشتغل فعلياً (بصراحة)

- **محرك البحث والجواب:** فهرس BM25 مكتوب من الصفر (`src/lib/luau/search.ts`) مع
  معالجة عربية خاصة (تشكيل، همزات، سوابق، جذور) في `src/lib/luau/text.ts`.
- **المعرفة:** دروس ومراجع وأنظمة كاملة مكتوبة بالعناية، زائد ما يضيفه المساهمون
  (معارف، كتب) وصفحات يقرأها الزاحف من مصادر مجانية.
- **التدريب الآلي:** منهج 129 موضوعاً (Luau + Python/JS/C++ أساسيات + تفكير/صناعة)
  يولّد دروسه عبر Qwen ويخزّنها في قاعدة المعرفة. يعمل من GitHub Actions كل 20
  دقيقة (بدون الحاجة لجهازك) — شوف `.github/workflows/train.yml`.
- **مساهمة الأجهزة:** جهاز الزائر يعالج نصوصاً من مكتبة المعرفة ويرسل عدد
  الكلمات وبصمة تحقّق. هذه **مساهمة معالجة ومقياس مشاركة** — ليست تدريباً
  لشبكة عصبية ولا تعديلاً لأوزان نموذج.
- **الموافقة:** لا يشتغل أي شي على جهاز الزائر قبل ما يوافق (`src/lib/consent.ts`)،
  وفي صفحة تفاصيل على `/privacy`.

## التقنيات

Next.js 16 (App Router) · React 19 · TypeScript · Tailwind CSS 4 · Drizzle ORM · PostgreSQL (Neon)

## التشغيل محلياً

```bash
npm install

# المتغيرات في ملف .env.local (والأمانات كمستودع سري على GitHub):
#   DATABASE_URL=postgresql://...(Neon)...
#   QWEN_ENABLED=true
#   QWEN_API_KEY=sk-...

npm run db:push   # إنشاء الجداول (الإعدادات تقرأ من .env.local)
npm run dev       # http://localhost:3000
```

للتحقق من الكود بدون تشغيل: `npm run typecheck` و `npm run lint`.

> **التدريب السحابي:** إذا كان الـ cron يعمل من GitHub، اجعل `TRAINER_SKIP_LOCAL=true`
> في `.env.local` حتى لا يدرّب جهازك دروساً مكررة.

## البنية

```
src/app          الصفحات ونقاط الـ API
src/components   واجهات React
src/lib/luau     المحرك: البحث، النصوص، الدروس، القوالب، الزاحف، المدرب الآلي
src/db           جداول Drizzle
scripts/         أدوات مستقلة (أهمها train-cron.ts للتدريب السحابي)
.github/workflows  جدولة التدريب
```

## للمساهمين

- نقاط الكتابة عليها حد طلبات بسيط (`src/lib/rateLimit.ts`).
- ما في نظام حسابات بعد؛ هوية الجهاز عشوائية ومخزّنة بالمتصفح، فالتصويت
  والمساهمات قابلة للتلاعب — لا تعتمد عليها كمقياس نهائي.
- المحتوى المضاف يظهر لكل الزوار، فلا تضيف مواد محمية بحقوق نشر.

الرخصة: MIT (شوف `LICENSE`).
